<Window x:Class="iTunesSyncer.FileAccess.Dialog.AddDirectoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:iTunesSyncer.FileAccess.Dialog"
        mc:Ignorable="d"
        Title="新しいディレクトリの作成" Height="114" Width="321" ResizeMode="NoResize" SizeToContent="Height">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0"
            Content="新しいディレクトリの名前："/>

        <TextBox Grid.Row="1"
                 Height="22" Margin="0 10 0 10"
                 x:Name="textbox"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"
                    Margin="0 0 5 0"
                    Content="OK"
                    Command="{Binding OkCommand}" Click="Button_ClickOK"/>
            <Button Grid.Column="1"
                    Content="キャンセル"
                    Command="{Binding CancelCommand}" Click="Button_ClickCancel"/>
        </Grid>
    </Grid>
</Window>
